import React from 'react';
import ExchangeHandler from '@/src/features/auth/components/ExchangeHandler';

export const metadata = {
  title: 'Exchanging Credentials | CareerX Workspace',
};

export default function ExchangePage() {
  return <ExchangeHandler />;
}
