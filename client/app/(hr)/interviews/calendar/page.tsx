import React from 'react';
import { SlotCalendar } from '@/src/features/hr-interviews/components/SlotCalendar';

export const metadata = {
  title: 'Slots Calendar | CareerX Workspace',
};

export default function CalendarPage() {
  return <SlotCalendar />;
}
